venv/
__pycache__/

# 훈련용 이미지 폴더 및 대용량 파일 무시 (모든 버전 포함)
new_synthetic_plates/
new_synthetic_plates_v2/
new_synthetic_plates_v2.zip
synthetic_plates*/
test_data_cropped_plates/
test_data_full_car/

# 1차 파인튜닝 모델 가중치 파일 무시 (코랩에서 다운받은 대용량 파일)
models/inference_model/

# 모든 압축 파일 무시 (용량 폭발 방지)
*.zip

# Dataset
new_raw_images/
rec_train_images/

# --- Dataset & Image Files ---
*.jpg
*.jpeg
*.png
*.bmp

# (선택) 만약 특정 데이터셋 폴더 통째로 무시하려면 폴더명 추가
# datasets/
# images/

# --- Presentation / Chart Outputs ---
# 스크립트로 자동 생성된 차트 이미지들 제외
presentation_chart_*.png